{

  "title":"Dataset",

  "type": "object",

  "properties": {
    "additionalType": {
      "description" : "Fill in an url to an EDAM term",
      "oneOf": [
        {
          "type":"string",
          "format":"uri",
          "enum":["EDAM/Format"]
        },
        {
          "type":"string",
          "enum":["EDAM/Format"]
        }
      ]
    }
  },
  "required":[
    "additionalType"
  ]
}